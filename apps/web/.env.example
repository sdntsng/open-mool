# Example environment variables for the Open Mool web application
# Copy this file to .env.local and fill in your values if needed

# ============================================================
# AUTH0 AUTHENTICATION (Required)
# ============================================================
AUTH0_SECRET='use [openssl rand -hex 32] to generate a 32 bytes value'
AUTH0_BASE_URL='http://localhost:3000'
AUTH0_ISSUER_BASE_URL='https://YOUR_DOMAIN.auth0.com'
AUTH0_CLIENT_ID='YOUR_CLIENT_ID'
AUTH0_CLIENT_SECRET='YOUR_CLIENT_SECRET'

# ============================================================
# FUTURE API INTEGRATION (Optional)
# ============================================================
# If you integrate with the Open Mool API or external services
# in the future, you may need to add variables like:

# NEXT_PUBLIC_API_URL=https://api.openmool.org
# API endpoint for the Open Mool backend

# ============================================================
# NOTES
# ============================================================
# - Variables prefixed with NEXT_PUBLIC_ are exposed to the browser
# - Other variables are only available during build time
# - Never commit .env.local or files containing secrets to git
